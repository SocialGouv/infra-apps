apiVersion: v2
name: iterion
version: 0.0.0
description: Iterion cloud (workflow engine) — server + runner + bundled Mongo/NATS/MinIO
dependencies:
  - name: iterion
    repository: oci://ghcr.io/socialgouv/charts
    # 0.21.0: adds config.marketplace.enabled → ITERION_CLOUD_MARKETPLACE (the
    # public bot marketplace toggle) + checksum/config pod-reload annotations on
    # the server+runner deployments (so OIDC/marketplace config changes roll the
    # pods without a manual restart). 0.17.2 had completed the FULL k8s sandbox
    # on the runner (runner.sandbox.enabled → ITERION_POD_IP + sandbox RBAC +
    # per-run Secrets RBAC for `as: file` secrets / proxy TLS CA).
    version: 0.21.0
  # --- ovh-prod community datastores (replace the iterion subchart's deprecated
  # Bitnami mongodb+minio). Their values live at top-level `mongodb:` / `minio:`
  # (distinct from `iterion.mongodb` / `iterion.minio` which gate the bundled
  # Bitnami subcharts — disabled in values.ovh-prod.yaml).
  # ovh-dev keeps the Bitnami subcharts on (conditions below stay false there).
  - name: mongodb
    repository: https://groundhog2k.github.io/helm-charts/
    version: 0.7.8
    condition: mongodb.enabled
  - name: minio
    repository: https://charts.min.io/
    version: 5.4.0
    condition: minio.enabled
